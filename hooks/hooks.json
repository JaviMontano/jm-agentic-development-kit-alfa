{
  "hooks": [
    {
      "type": "SessionStart",
      "command": "bash scripts/session-init.sh",
      "description": "System integrity check + workspace detection + structured output for model greeting"
    },
    {
      "type": "UserPromptSubmit",
      "command": "bash scripts/user-prompt-filter.sh",
      "description": "Injection pattern detection (stderr) + workspace signal emission (stdout)"
    },
    {
      "type": "PreToolUse",
      "command": "bash scripts/pre-tool-guard.sh",
      "description": "Block dangerous commands (rm -rf, git reset --hard, .env access) before execution"
    },
    {
      "type": "PostToolUse",
      "command": "bash scripts/post-tool-check.sh",
      "description": "Auto-log tool usage to active workspace tasklog via workspace-manager.sh"
    },
    {
      "type": "Stop",
      "command": "bash scripts/stop-validator.sh",
      "description": "Mark session boundary in workspace tasklog + update timestamps"
    }
  ]
}
