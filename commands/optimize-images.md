---
description: "Optimize images: WebP conversion, lazy loading, srcset"
user-invocable: true
---

# /jm-adk:optimize-images

## Purpose

Optimize images: WebP conversion, lazy loading, srcset

## Arguments

| Argument | Description | Required | Default |
|----------|-------------|----------|---------|
| `input` | Primary input for the command | No | interactive |

## Examples

```bash
/jm-adk:optimize-images
/jm-adk:optimize-images input="your input here"
```

## Related Commands

- `/jm-adk:menu` — Full command palette
- `/jm-adk:search` — Find relevant skills
