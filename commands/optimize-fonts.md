---
description: "Optimize fonts: subsetting, preload, FOUT prevention"
user-invocable: true
---

# /jm-adk:optimize-fonts

## Purpose

Optimize fonts: subsetting, preload, FOUT prevention

## Arguments

| Argument | Description | Required | Default |
|----------|-------------|----------|---------|
| `input` | Primary input for the command | No | interactive |

## Examples

```bash
/jm-adk:optimize-fonts
/jm-adk:optimize-fonts input="your input here"
```

## Related Commands

- `/jm-adk:menu` — Full command palette
- `/jm-adk:search` — Find relevant skills
