---
description: "Deploy to Hostinger via SSH+Git"
user-invocable: true
---

# /jm-adk:ssh-deploy

## Purpose

Deploy to Hostinger via SSH+Git

## Arguments

| Argument | Description | Required | Default |
|----------|-------------|----------|---------|
| `input` | Primary input for the command | No | interactive |

## Examples

```bash
/jm-adk:ssh-deploy
/jm-adk:ssh-deploy input="your input here"
```

## Related Commands

- `/jm-adk:menu` — Full command palette
- `/jm-adk:search` — Find relevant skills
